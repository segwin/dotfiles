[alias]
  permission-reset = !git diff -p -R --no-ext-diff --no-color | grep -E '^(diff|(old|new) mode)' --color=never | git apply
[merge]
  ff = true
[user]
  name = Eric Seguin
  email = eric@segwin.ca
#[url "git@github.com:"]
#  insteadOf = https://github.com/
# note: can use ~/.netrc to replace insteadOf, format is:
# > machine gitlab.com
# > login my-username
# > password my-access-token
